{
  "name": "express-payload-sanitizer",
  "version": "1.0.0",
  "description": "Express Payload Sanitizer is a middleware package for Express.js that provides two essential utilities: - **`cleanPayload`**: Cleans `req.body` by removing empty strings, `null` values, and empty objects. - **`restrictRequest`**: Validates `req.body` to ensure no `null`, `undefined`, or invalid `NaN` values are present and return bad request as response to client side.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Sanitizer",
    "Express Payload Sanitizer",
    "Payload Sanitizer",
    "Body Sanitizer"
  ],
  "author": "Mihrab Miah",
  "license": "MIT"
}
